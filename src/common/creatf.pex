/* creatF(file$p, buf$p, size$w, mode$b)
   set files buffer and buffer size
   read into buffer if mode$b =    1
*/

creatF: PROCEDURE(fp, buf, bsize, mode) external;
	DECLARE (fp, buf, bsize) ADDRESS, mode BYTE;
end;
