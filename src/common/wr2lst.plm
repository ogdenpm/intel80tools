wr2cLst: DO;

$include(:f3:wr2lst.ipx)

wr2cLst: procedure(arg1w) public;
	declare arg1w address;
	declare pbp address;
	declare bp based pbp(2) byte;
	pbp = .arg1w;
	call wrcLst(bp(1));
	call wrcLst(bp(0));
end;
end;
