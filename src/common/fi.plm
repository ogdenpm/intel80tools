fi: DO;
$include(:f3:fi.ipx)

findInfo: PROCEDURE PUBLIC;
	DECLARE i ADDRESS;

	if cursym.info$p= 0 then
	do;
		curInfo$p = 0;
		return;
	end;
	i = blockDepth;
	do while i <> 0;
		call findScopedInfo(procChains(i));
		if curInfo$p <> 0 then
			return;
		i = i - 1;
	end;
end;


END;
