cpyflg: do;

$include(:f3:cpyflg.ipx)

cpyFlags: PROCEDURE(base) PUBLIC;
	DECLARE base ADDRESS;
	DECLARE b BASED base(3) BYTE;
	DECLARE i BYTE;

	do i = 0 to 2;
		info.flag(i) = b(i);
	end;
end;



end;

