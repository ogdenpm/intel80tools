silit: DO;
declare curInfo$p address external,
	litinfo BASED curInfo$p STRUCTURE(len BYTE, type BYTE, sym ADDRESS,
				 scope ADDRESS, ilink ADDRESS, litAddr ADDRESS);

setLitaddr: PROCEDURE(litaddr) PUBLIC;
	DECLARE litaddr ADDRESS;
	litinfo.litAddr = litaddr;
end;

end;
